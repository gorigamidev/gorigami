<main id="not-found-page">
	<div class="error-container">
		<span class="ghost-number" aria-hidden="true">404</span>
		<img
			src="https://res.cloudinary.com/gorigami/image/upload/f_auto,q_auto/v1769109708/gorigamiland/EnjoyWork_lgbnej.png"
			alt="Lost Gorilla"
			loading="lazy"
			decoding="async"
			width="180"
			height="180"
		/>
		<div class="error-content">
			<h1 class="error-title" data-i18n="notFound.title">Oops! You're lost.</h1>
			<p class="error-desc" data-i18n="notFound.desc">
				We can't seem to find the page you're looking for.
			</p>
			<a href="/" class="btn-home" data-i18n="notFound.cta">→ Return Home</a>
		</div>
	</div>
</main>

<style>
	/* Hallmark · genre: modern-minimal · a small on-brand moment, not a generic centred block */
	#not-found-page {
		min-height: 70vh;
		width: 100%;
		display: flex;
		justify-content: center;
		align-items: center;
		position: relative;
		padding: var(--space-2xl) 10%;
		background-color: var(--smoke, #f0f0ef);
		font-family: var(--body-font, sans-serif);
		overflow: hidden;
	}

	.error-container {
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
		gap: var(--space-sm);
		max-width: 480px;
		position: relative;
		z-index: 1;
	}

	.ghost-number {
		position: absolute;
		top: -2rem;
		left: 50%;
		transform: translateX(-50%);
		font-family: var(--heading-font, sans-serif);
		font-size: 10rem;
		font-weight: 900;
		line-height: 1;
		color: var(--lavender-mist);
		z-index: 0;
		user-select: none;
	}

	#not-found-page img {
		height: auto;
		width: 100%;
		max-width: 140px;
		object-fit: contain;
		position: relative;
	}

	.error-title {
		font-family: var(--heading-font, sans-serif);
		font-size: 2.5rem;
		font-weight: 800;
		color: var(--midnight-violet, #16091d);
		line-height: 1.2;
		margin-bottom: var(--space-2xs);
	}

	.error-desc {
		color: var(--mid-gray, #6b6b6b);
		font-size: 1.1rem;
		margin-bottom: var(--space-sm);
		max-width: 380px;
		margin-left: auto;
		margin-right: auto;
	}

	.btn-home {
		display: inline-block;
		padding: 1rem 2.5rem;
		font-weight: 600;
		font-size: 1rem;
		color: var(--paper-white, #ffffff);
		background: var(--midnight-violet, #16091d);
		border: 2px solid var(--midnight-violet, #16091d);
		text-decoration: none;
		transition: background-color 0.3s var(--ease-out, ease), color 0.3s var(--ease-out, ease), transform 0.3s var(--ease-out, ease);
		cursor: pointer;
	}

	.btn-home:hover {
		background: transparent;
		color: var(--midnight-violet, #16091d);
		transform: translateY(-2px);
	}

	@media screen and (max-width: 768px) {
		.ghost-number {
			font-size: 6rem;
			top: -1rem;
		}

		.error-title {
			font-size: 2rem;
		}

		#not-found-page img {
			max-width: 110px;
		}

		.error-desc {
			font-size: 1rem;
		}
	}
</style>
