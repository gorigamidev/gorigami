<main id="hero">
    <div class="hero-img">
        <img class="full" src="/img/gorilla-pc.jpg" alt="Gorilla Developer">
        <img class="mobile" src="/img/gorilla-mobile.jpg" alt="Gorilla Developer">        
    </div>
    <div class="hero-content">
        <h1>¡Renueva tu presencia en línea con Landing-Pages ultrarrápidas y optimizadas para SEO!</h1>
        <h3>En <span>gorigami,</span> creamos landings increíblemente rápidas y optimizadas para SEO en solo 10 días, todo por un precio inmejorable de $450.000 COP. <br/>¡Eleva tu marca hoy!
        </h3>
        <div id="actions">
            <a href="https://wa.me/573057197012?text=¡Hola!%20Estoy%20interesado%20en%20crear%20una%20Landing%20con%20ustedes" target="_blank" class="hero-button now">¡Empieza hoy mismo!</a>
            <a href="#details" class="hero-button more">Conoce más</a>
        </div>
    </div>
</main>
<style>
    #hero{
        height: 100vh;
        width: 100%;
        display: flex;
        flex-direction: row-reverse;
        justify-content: center;
        align-items: center;
        padding: 0 10%;
        position: relative;
        overflow: hidden;
        gap: 1rem;
    }
    .hero-img{
        position: relative;
        height: 100%;
        display: flex;
        justify-content: center;
        align-items: center;
    }
    .hero-img .full{
        display: block;
    }
    .hero-img .mobile{
        display: none;
    }
    .hero-img img{
        width: 90%;
        object-fit: cover;
        border-radius: 2rem;
        box-shadow: 0 0 2rem 0 rgba( 31, 38, 135, 0.37 );
    }
    .hero-content{
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: left;
        width: 60%;
        gap: 2rem;
    }
    .hero-content h1{
        font-size: 2rem;
        font-weight: 700;
        color: #fff;
        line-height: 1.1;
    }
    .hero-content h3{
        font-size: 1.1rem;
        font-weight: 400;
        line-height: 1.5;
        margin-bottom: 2rem;
        padding-right: 6rem;
    }
    .hero-content span{
        font-family: var(--primary-font);
        font-size: 1.3rem;
        color: var(--purple-color);
    }
    #actions{
        display: flex;
        flex-direction: row;
        gap: 2rem;
    }
    .hero-button{
        text-align: center;
        width: 18rem;
        height: 3.5rem;
        padding: 1rem;
        border-radius: 1rem;
        text-decoration: none;
        font-weight: bold;
        box-shadow: 0 .5rem 2rem 0 rgba( 31, 38, 135, 0.37 );
    }
    .hero-button.now{
        color: var(--dark-color);
        background: var(--orange-color);
        outline: var(--orange-color) .3rem solid;
    }
    .hero-button.now:hover{
        background: transparent;
        color: #fdb74d;
        outline: var(--orange-color) .3rem solid;
        transform: translateY(-.3rem);
        box-shadow: 0 .5rem 2rem 0 rgba( 51, 78, 185, 0.7 );
    }
    .hero-button.more{
        width: 10rem;
        color: var(--purple-color);
        background: transparent;
        outline: var(--purple-color) .3rem solid;
    }
    .hero-button.more:hover{
        color: var(--dark-color);
        background: var(--purple-color);
        transform: translateY(-.3rem);
        box-shadow: 0 .5rem 2rem 0 rgba( 51, 78, 185, 0.7 );

    }
    @media screen and (max-width: 700px){
        #hero{
            height: 100dvh;
            width: 100%;
            display: flex;
            flex-direction: column-reverse;
            justify-content: center;
            align-items: center;
            padding: 0;
            position: relative;
            overflow: hidden;
        }
        .hero-img{
            position: absolute;
            border-radius: 0;
            bottom: 8rem;
            left: 0;
            height: 100%;
            width: 100%;
            z-index: 2;
            margin: auto 0;
            box-shadow: none;
            opacity: .85;
        }
        .hero-img .full{
            display: none;
        }
        .hero-img .mobile{
            position: fixed;
            display: block;
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center bottom;
            border-radius: 0;
            z-index: 1;
        }
        .hero-content{
            background: rgba(22, 16, 31, 0.5);
            margin: .2rem;
            padding: 1rem;
            position: absolute;
            bottom: 5rem;
            z-index: 3;
            width: 99%;
            margin: auto;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            gap: 1rem;
            box-shadow: 0 0 2rem 0 rgba( 31, 38, 135, 0.37 );
            backdrop-filter: blur( 3px );
            border-radius: 1rem;
            border: 1px solid rgba( 255, 255, 255, 0.18 );
        }
        .hero-content h1{
            font-size: 1.6rem;
        }
        .hero-content h3{
            font-size: 1rem;
            padding: 0 1.2rem;
        }
        .hero-content span{
            font-size: 1rem;
        }
        #actions{
            gap:1rem;
        }
        .hero-button{
            width: 10rem;
            height: 4rem;
            font-size: 1rem;
            padding: .7rem;
            border-radius: 1rem;
        }
        .hero-button.more{
            padding: 1.3rem;
        }
    }
</style>